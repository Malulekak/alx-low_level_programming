#include "main.h"
#include <stdio.h>

/**
  * add - Adds two values
  * @k: The first number to be computed
  * @m: The second number to be computed
  * Return: Integer value
  */

int add(int k, int m)
{
	return (k + m);
}
